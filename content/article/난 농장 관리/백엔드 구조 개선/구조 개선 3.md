---
date: '2026-09-12T19:30:00+09:00'
draft: true
title: '백엔드 구조 개선 3: 테스트를 넘어 운영 데이터까지 전환하기'
linkTitle: '운영 데이터까지 전환하기'
weight: 3
---

난 묶음의 수량과 위치를 바꾸는 경로를 `Mutation Engine`으로 통합하고 단위 테스트도 추가했다. 하지만 코드 테스트가 통과했다는 이유만으로 실제 운영 DB를 새로운 구조로 바꿀 수는 없었다.

이번 작업에서는 기존 난 묶음에 `revision`이라는 상태 버전 번호를 부여하고, 과거 변경을 `Mutation Ledger`로 옮겨야 했다. 전환 이후에는 엔진을 거치지 않고 난 묶음을 직접 수정하는 코드도 차단해야 했다.

따라서 코드가 예상대로 동작하는지뿐 아니라 기존 운영 데이터가 새로운 규칙을 만족하는지와, 운영에서 사용하는 PostgreSQL이 동시에 들어오는 요청을 어떻게 처리하는지도 확인했다.

## 실제 PostgreSQL에서 드러난 동시성 문제

기존 테스트에서 사용하던 가벼운 테스트용 DB만으로는 데이터 잠금과 제약 조건을 충분히 확인하기 어려웠다.

리팩터링 과정에서는 다음과 같은 문제가 발견됐다.

* `Settlement` 최초 설정을 동시에 만들면 두 요청이 같은 데이터를 생성하며 UNIQUE 제약 조건과 충돌했다.
* 품종과 자재 코드를 동시에 발급하면 같은 번호를 선택할 가능성이 있었다.
* 하나의 `Mutation`에서 여러 난 묶음을 만들 때 변경 정보를 보관하는 write context가 서로 섞였다.
* 동일한 `Work Command`가 동시에 실행될 수 있었다.
* 이미 적용한 `Work Effect`가 다시 실행돼 수량이 두 번 바뀔 수 있었다.

이 문제들은 메서드를 한 번씩 순서대로 호출하는 테스트에서는 나타나지 않았다. 테스트용 DB와 PostgreSQL은 잠금, 트랜잭션과 중복 데이터 제한을 처리하는 방식에도 차이가 있었다.

따라서 PostgreSQL을 사용하는 별도 테스트 환경에서 다음 조건을 직접 검증했다.

* 같은 행을 바꾸는 병렬 요청이 row lock으로 한 요청씩 처리되는지
* 여러 난 묶음을 함께 바꿀 때 항상 같은 lock 순서를 사용하는지
* UNIQUE constraint 충돌을 올바르게 처리하는지
* 작업 중간에 실패하면 전체 transaction이 rollback되는지
* 같은 `Command`와 `Effect`가 동시에 들어와도 멱등성이 보장되는지

중요한 것은 테스트의 개수가 아니라 운영에서 사용하는 DB의 동작을 같은 환경에서 검증하는 것이었다.

## 운영 백업을 이용한 migration rehearsal

테스트를 위해 만든 데이터만 사용하면 실제 운영 DB에 오랫동안 쌓인 예외를 놓칠 수 있다.

운영 DB의 백업을 별도 PostgreSQL에 복원하고 실제 전환과 같은 순서로 migration rehearsal, 즉 전환 예행연습을 진행했다.

```text
운영 백업 복원
→ Flyway migration
→ 과거 state chain 생성
→ Engine ACTIVE 전환
→ DB write fence 적용
→ 주요 작업 실행
→ UI·API smoke test
→ reconciliation
```

과거 난 묶음은 복원할 수 있는 작업 이력을 기준으로 `BASELINE`, `CREATE`, `CHANGE`, `DELETE`로 이어지는 state chain으로 변환했다. 각 체인의 revision이 중간에 끊기지 않는지, 마지막 snapshot과 현재 난 묶음이 같은지를 전체 데이터에서 검사했다.

검사 결과가 다음 조건을 만족해야만 새 엔진을 활성화하도록 했다.

```text
ready=true
issues=[]
```

문제가 하나라도 남아 있으면 다음 단계로 진행하지 않았다.

![격리 복원 DB의 상태 변경 엔진 전환 예행연습](engine-cutover-rehearsal.svg "운영 백업을 별도 PostgreSQL에 복원해 이력 적재, 전환과 주요 기능을 검증했다.")

## migration으로 발견한 숨은 쓰기 경로

전환 예행연습에서는 단위 테스트에서 발견하지 못한 기존 데이터 변경 경로도 확인됐다.

기존 `Inbound` 코드는 난 묶음을 먼저 INSERT한 뒤 일부 값을 UPDATE했다. Legacy 구조에서는 정상적으로 동작했지만, Engine-only 상태에서 직접 UPDATE를 차단하는 DB write fence와 충돌했다.

처음 INSERT할 때 필요한 값을 모두 넣도록 변경해 두 번의 쓰기를 하나의 INSERT로 정리했다.

이 문제는 데이터 이전용 SQL의 오류가 아니었다. 새로운 보호 규칙을 실제 운영 흐름에 적용하면서 기존 코드에 남아 있던 우회 경로가 드러난 경우였다.

운영 백업을 이용한 예행연습은 과거 데이터를 변환할 수 있는지만 확인하는 과정이 아니었다. 새 구조에서 허용되지 않는 애플리케이션의 데이터 변경 경로까지 찾는 역할을 했다.

## Engine ACTIVE 이후의 복구 전략

일반적인 애플리케이션 배포에서는 새 버전에 문제가 생기면 이전 버전을 다시 실행해 복구할 수 있다.

하지만 DB를 `Engine ACTIVE` 상태로 전환한 뒤에는 예전 애플리케이션만 다시 실행해서는 안 된다. Legacy writer는 난 묶음의 현재 값만 바꾸고 `Mutation Ledger`를 남기지 않기 때문이다.

```text
DB는 Engine ACTIVE
+
애플리케이션은 Legacy writer
= 현재 상태와 Mutation Ledger가 어긋날 수 있음
```

따라서 ACTIVE 이후 문제가 생기면 수정한 새 버전을 다시 배포하는 roll-forward를 기본 대응으로 정했다. DB까지 되돌려야 한다면 애플리케이션 이미지뿐 아니라 전환 직전의 DB와 당시 배포 설정을 함께 복원해야 했다.

실제 전환 절차는 다음과 같이 정리했다.

```text
Backend 중지
→ 운영 DB 최종 backup
→ Engine candidate DB 복원
→ schema·row·ledger 검증
→ 기존 운영 DB 이름 변경
→ candidate DB를 production 이름으로 변경
→ writer version ConfigMap 갱신
→ 새 Backend 이미지 시작
→ smoke test
→ reconciliation
```

기존 운영 DB는 바로 삭제하지 않고 다른 이름으로 보존했다. 전환 직전 DB 백업과 당시의 배포 설정도 함께 남겨 애플리케이션과 데이터의 조합을 다시 만들 수 있도록 했다.

## 코드량보다 책임과 조회 비용

이번 리팩터링은 전체 코드 줄 수를 줄이기 위한 작업은 아니었다. 자동 코드 정리의 영향을 제외하면 전체 코드량이 크게 감소했다고 보기도 어렵다.

대신 여러 책임이 섞여 있던 코드와 반복 조회에서는 변화가 있었다.

작업 결과의 DB 조회, 과거 데이터 해석과 화면 표시를 모두 담당하던 `WorkOperationDetailService`는 408줄에서 89줄로 줄었다. 통계 조회와 화면 응답 조립을 함께 담당하던 `AnalyticsQueryService`도 201줄에서 156줄로 책임을 줄였다.

같은 정보를 조회할 때 실행되는 SQL 수도 함께 측정했다.

```text
사용자 난 묶음 그룹 목록 50건
101회 → 약 3회

작업 종류별 통계
6회 → 2회

품종별 재고 통계
2회 → 1회
```

반면 일부 `Analytics` 조회는 다른 모듈의 QueryDSL 타입을 직접 사용하지 않도록 경계를 나누면서 SQL 실행 횟수가 증가했다. 모든 결과를 성능 개선이라고 묶지 않고, 모듈 경계와 조회 비용 사이의 trade-off도 함께 기록했다.

![백엔드 리팩터링 검증 결과](리팩터링-검증-결과.svg "모듈 사이의 직접 의존, Analytics 조회 SQL과 리팩터링 완료 시점의 테스트 결과다.")

## 단계별 검증 범위

작업을 시작할 때는 기본 테스트 190개가 통과하고 49개는 실행되지 않는 상태였다.

구조 리팩터링을 마친 시점에는 기본 테스트 459개, 실제 PostgreSQL을 사용하는 테스트 98개와 benchmark 2개가 통과했다. 실패하거나 실행에서 제외된 테스트는 없었다.

이후 `Mutation Engine`의 전환 예행연습 시점에는 기본 테스트 460개와 PostgreSQL 테스트 96개가 통과했다. 두 종류를 합친 백엔드 테스트는 556개였다.

테스트 수가 늘어난 것만으로 구조의 안전성을 증명할 수는 없다. 서로 다른 단계에서 서로 다른 문제를 확인하도록 검증 범위를 나누었다.

```text
업무 규칙 테스트
→ PostgreSQL에서 통합·동시 요청 테스트
→ 운영 백업을 이용한 전환 예행연습
→ 주요 화면과 API 기능 확인
→ 현재 데이터와 변경 이력 전체 검사
```

테스트 코드가 예상한 상황과 실제 운영 데이터에 이미 존재하는 상태를 함께 검사한 뒤 전환을 진행했다.

## Engine-only 전환 이후

최종적으로 실제 운영 PostgreSQL을 Legacy writer에서 `Engine-only` 구조로 전환했다. 전환 후에는 `ACTIVE`, `ready=true`, `issues=[]`를 확인했다.

운영 이전 DB와 전환 직전 백업도 복구 자료로 별도 보존했다.

이번 작업을 통해 코드 안에서 책임을 나누는 것과 운영 데이터를 새로운 구조에 맞게 옮기는 것은 별개의 일이라는 점을 확인했다. 코드 테스트가 통과해도 기존 데이터의 이력이 끊기거나, 이전 애플리케이션이 새로운 DB 규칙을 우회하면 전환은 끝난 것이 아니다.

구조 개선의 원래 목적은 새로운 기능을 추가할 때 기존 코드의 수정 범위를 줄이는 것이었다. 따라서 실제 성공 여부는 다음 작업 유형을 추가할 때 확인할 수 있다.

* 새로운 Handler만 추가하면 되는가
* 기존 switch와 조건문이 다시 길어지는가
* Mutation Engine의 공통 처리까지 수정해야 하는가
* 다른 모듈의 내부 구현을 알아야 하는가
* 기존 파일을 몇 개나 함께 수정하는가

앞으로는 새로운 기능을 추가할 때 이 항목을 기록하려 한다. Mutation API의 p50과 p95, transaction 시간과 lock wait도 측정하고, reconciliation은 전환 시점의 일회성 검사에서 끝내지 않고 정기적인 invariant 검사로 이어갈 예정이다.

좋은 아키텍처인지는 사용한 설계 패턴의 수보다 다음 변경에서 확인해야 한다. 이번 작업은 새로운 구조를 완성한 기록이라기보다, 변경 범위와 검증 비용을 측정할 수 있는 출발점을 만든 과정에 가깝다.
