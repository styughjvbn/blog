# 블로그 작성 지침

이 저장소에서 글을 새로 작성하거나 수정할 때 적용하는 공통 규칙이다. 특정 프로젝트나 시리즈의 작성 메모는 해당 글에서만 다루며 이 파일에 일반 규칙처럼 추가하지 않는다. 아래의 프로젝트명과 용어는 규칙을 설명하기 위한 예시일 뿐 모든 글에서 사용해야 하는 형식이 아니다. 별도의 사용자 지시가 있다면 사용자 지시를 우선한다.

## 기본 문체

- 한국어 기술 기록과 회고 형식으로 작성한다.
- 실제로 겪은 상황, 문제, 판단, 변경과 결과를 시간 또는 인과관계가 드러나는 순서로 설명한다.
- 과장된 표현, 홍보 문구와 불필요한 감상보다 구체적인 상황과 판단 근거를 우선한다.
- 문장은 짧고 직접적으로 쓴다. 한 문장에 여러 판단을 억지로 넣지 않는다.
- 독자에게 말을 거는 표현보다 작성자가 경험을 정리하는 서술형 문체를 사용한다.
- 본문은 `~했다`, `~였다`, `~할 수 있었다`와 같은 회고형 종결을 자연스럽게 사용한다.
- 같은 설명과 결론을 여러 문단에서 반복하지 않는다.

## 제목과 소제목

- 제목은 글에서 다루는 문제나 변화가 드러나도록 작성한다.
- 시리즈 글은 기존 시리즈의 번호, 제목과 `linkTitle` 형식을 따른다.
- 소제목은 `~했다`, `~이다`, `~였다`처럼 완결된 문장형 어미로 끝내지 않는다.
- 소제목은 주제나 쟁점을 나타내는 짧고 담백한 명사구로 작성한다.
- 감정적이거나 문학적인 표현, 의미를 과장하는 수사와 억지로 호기심을 유도하는 문구를 피한다.
- `드러난 문제`, `남겨진 과제`, `빈칸`, `잘못된 결론`, `~앞의 선택`처럼 서사를 강조하는 표현보다 다루는 기술 내용을 직접 적는다.
- 예를 들어 `하루 한 번의 snapshot이 남긴 빈칸`보다 `백업 주기와 복원 한계`, `기본키 비교로 내린 잘못된 결론`보다 `기본키 기준 비교의 오류`처럼 작성한다.
- 소제목만 읽어도 글의 흐름을 파악할 수 있도록 한다.

## 독자의 사전 지식

- 독자가 해당 프로젝트와 도메인을 처음 접한다는 전제로 작성한다.
- 글을 이해하는 데 필요한 개념은 처음 등장할 때 짧게 설명한다.
- 예를 들어 농장 관리 글에서 `난 묶음`이 핵심 단위라면, 같은 품종과 비슷한 상태의 화분을 함께 관리하는 단위라는 설명을 먼저 제공한다.
- 전문 용어와 고유 명칭은 무조건 쉬운 말로 바꾸지 않는다. 쉬운 설명과 실제 명칭을 함께 제공한다.
- 코드 구조를 설명할 때는 실제 모듈, 클래스, 테이블과 명령의 이름을 지나치게 추상화하지 않는다.
- 예를 들어 `농장 재고 영역`이라고만 쓰면 실제 코드 구조가 사라지는 문맥에서는 `농장 재고를 담당하는 Farm 모듈`처럼 설명과 코드 명칭을 함께 쓴다.
- 실제 이름이 글의 논지를 이해하는 데 필요하면 백틱으로 감싸 정확하게 표기한다.
- 논지와 관계없는 내부 타입과 파일명은 나열하지 않는다.

## 글의 구성

- 기능 목록만 나열하기보다 다음 흐름을 우선한다.

```text
기존 상황
→ 실제로 발생한 문제
→ 처음 시도하거나 검토한 방법
→ 그 방법의 한계
→ 선택한 해결책
→ 검증 방법
→ 결과와 남은 한계
```

- 모든 글에 위 구성을 기계적으로 적용하지 않는다. 글의 주제에 맞게 필요한 단계만 사용한다.
- 기술 선택은 일반적인 장점보다 당시 프로젝트의 규모, 사용 환경과 제약 안에서 판단한 이유를 설명한다.
- 기능을 추가한 내용뿐 아니라 실제로 시도했다가 제거한 구조와 제거 기준도 의미가 있다면 기록한다.
- 여러 글로 나눌 때는 각 글의 주제가 독립적인지, 읽는 순서가 실제 작업 흐름과 맞는지 확인한다.
- 서로 이어지는 작업을 분리해 시간 순서가 뒤집히거나 같은 설명이 반복된다면 하나의 글로 합친다.

## 사실과 해석

- 커밋, 테스트 결과, 측정값, 데이터, 문서와 사용자 확인 등 근거가 있는 내용만 단정적으로 작성한다.
- 추정이나 해석은 확인된 사실과 분리하고 `가능성이 있었다`, `정황으로 판단했다`처럼 표현한다.
- 중간에 철회된 결론을 최종 사실처럼 사용하지 않는다. 판단을 수정한 과정이 중요하다면 반례와 수정 이유를 함께 쓴다.
- 기록만으로 누가 문제를 발견했는지, 누가 수정을 지시했는지 또는 어떤 의도로 작업했는지를 추측하지 않는다.
- 숫자와 상태값은 가능한 한 최종 산출물을 기준으로 확인한다. 분석이나 작업이 다시 실행되면 기존 수치가 유효한지 다시 확인한다.
- 데이터만으로 설명할 수 없는 내용을 임의로 채우지 않는다. 사람의 확인으로 근거를 보완했다면 질문과 확인 과정을 정확히 표현한다.

## 한계와 실패 과정

- 성공 결과뿐 아니라 사용한 자료, 테스트와 분석이 확인할 수 없는 범위도 함께 설명한다.
- 한계는 `정확하지 않을 수 있다`처럼 추상적으로 쓰지 않고 어떤 조건에서 무엇을 알 수 없는지 구체적으로 적는다.
- 자동화된 결과도 원본 데이터나 실제 실행 결과와 대조한 방법을 설명한다.
- 문제가 남아 작업을 중단한 조건이나 사람의 판단이 필요했던 지점을 숨기지 않는다.
- 실패한 접근을 소개할 때는 단순 실패담으로 끝내지 않고 어떤 근거로 방향을 바꿨는지 연결한다.

## 수치와 검증 결과

- 테스트 개수, 코드 줄 수와 파일 수 자체를 품질의 증거로 사용하지 않는다.
- 수치가 무엇을 측정했고 어떤 판단을 뒷받침하는지 함께 설명한다.
- 좋아진 결과만 고르지 않고 변경으로 인해 늘어난 비용이나 남은 문제도 함께 기록한다.
- 비교 수치는 가능한 한 같은 조건에서 측정하고, 조건이 다르면 그 차이를 밝힌다.
- 여러 수치를 비교할 때는 표를 사용하고, 핵심 상태값은 짧은 코드 블록으로 제시할 수 있다.

## 코드 블록과 표

- 코드 블록은 긴 원문을 복사하기보다 흐름, 책임, 상태 변화와 핵심 예시를 보여주는 데 사용한다.
- 긴 JSON, SQL과 로그는 논지에 필요한 필드와 행만 추려 보여준다.
- 예를 들어 여러 상태 필드가 반복되는 manifest를 보여줄 때는 전체 원문 대신 식별자, 변경 전후 수량과 결과처럼 글의 주장에 필요한 값만 남긴다.
- 원본을 축약하거나 재구성한 예시는 그 사실을 독자가 알 수 있게 한다.
- 표는 여러 항목의 정확한 비교가 문장보다 명확할 때만 사용한다.
- 동일한 결과를 본문, 표와 코드 블록에서 반복하지 않는다.

## 이미지와 스크린샷

- 시각자료는 주장에 대한 근거를 보여주거나 복잡한 관계를 단순화할 때만 사용한다.
- 장식 목적의 이미지와 과도하게 인공적인 느낌의 삽화는 사용하지 않는다.
- 직접 만든 도식은 단순한 색상과 도형을 사용하고 불필요한 문구를 제거한다.
- 스크린샷은 전체 화면보다 설명에 필요한 영역만 잘라 사용한다.
- 긴 코드와 로그는 핵심 식별자, 변경 전후 값과 검증 결과가 보이는 범위만 담는다.
- 계정, 비밀번호, 토큰, Secret, 내부 주소와 공개하면 안 되는 정보는 가린다.
- 같은 내용을 보여주는 이미지를 반복해서 배치하지 않는다.
- 이미지 파일은 해당 글과 같은 page bundle 디렉터리에 두고 상대 경로로 연결한다.
- 대체 텍스트와 title에는 이미지가 보여주는 내용을 구체적으로 적는다.

## Hugo 문서 형식

- 기존 글의 front matter 형식을 따른다.

```yaml
---
date: 'YYYY-MM-DDTHH:MM:SS+09:00'
draft: true
title: '글 제목'
linkTitle: '짧은 제목'
weight: 1
---
```

- 시리즈 안에서 `weight`와 번호의 순서를 맞춘다.
- 목록과 코드 블록 앞뒤에는 빈 줄을 둔다.
- 이미지 링크는 `![설명](파일명 "보충 설명")` 형식을 사용한다.
- 작성 중인 메모나 이미지 위치는 HTML 주석으로 남기고, 작업이 끝나면 실제 내용으로 교체하거나 제거한다.
- 게시 전에는 남은 TODO, 깨진 상대 링크, 사용하지 않는 이미지와 폐기된 산출물 언급을 확인한다.

## 작업 범위와 검증

- 사용자가 지정한 글과 파일만 수정한다.
- 명시적으로 요청받지 않은 이미지 생성, 브라우저 미리보기와 computer use를 실행하지 않는다.
- 기존 작업 트리의 관련 없는 변경과 미추적 파일은 건드리거나 커밋하지 않는다.
- 커밋 요청을 받으면 이번 작업과 관련된 파일만 정확히 스테이징한다.
- 최소한 `git diff --check`로 형식 오류를 확인한다.
- 빌드나 시각 검증은 사용자가 요청했거나 변경 위험상 꼭 필요한 경우에만 수행한다.
